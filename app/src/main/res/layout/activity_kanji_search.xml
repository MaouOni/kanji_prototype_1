<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Search Input Field -->
    <EditText
        android:id="@+id/search_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search for Kanji..."
        android:padding="12dp"
        android:drawableEnd="@drawable/ic_draw_button" />

    <!-- OR Divider -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OR"
        android:textSize="18sp"
        android:layout_marginVertical="16dp"
        android:gravity="center_horizontal"
        android:layout_gravity="center_horizontal" />

    <!-- Draw Button -->
    <Button
        android:id="@+id/draw_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Draw Kanji"
        android:backgroundTint="@android:color/holo_blue_dark"
        android:textColor="@android:color/white"
        android:padding="12dp"
        android:layout_gravity="center_horizontal"
        android:drawableStart="@drawable/ic_draw_button" />

    <!-- Placeholder for RecyclerView (search results) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/kanji_results_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp"
        android:scrollbars="vertical" />

    <!-- Placeholder for Neural Network results (if implemented) -->
</LinearLayout>
